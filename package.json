{
  "scripts": {
    "test": "./gradlew test",
    "lint": "./gradlew ktlintCheck",
    "build": "./gradlew build",
    "prepare": "husky install",
    "preinstall": "./gradlew clean",
    "commit": "cz"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.2.0",
    "@commitlint/config-conventional": "^17.2.0",
    "commitizen": "^4.2.5",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^8.0.1"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
